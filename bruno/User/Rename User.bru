meta {
  name: Rename User
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/user/:id/rename
  body: json
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "newusername"
  }
}

docs {
  Renomme un utilisateur.
  
  Nécessite un token JWT valide.
}
